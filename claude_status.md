2B-3: DONE ✅ — Elementor Flip Box expansion. Trigger: E2E na skakaciehradyorava.sk (14 produktov, len 2 ceny — regresia z 3) — Elementor Flip Box widgety nie sú ani Swiper/Slick/Owl ani CSS overflow-x carousel, takže existujúci UI Expansion Layer ich preskočil; back layer (rotateY 180deg) s cenami + bullet detailmi (Kapacita/Rozmery/Výška/Vek) ostal neviditeľný. Modifikované: app/core/browser.py — 3 malé in-place úpravy + 1 nová metóda. (1) _detect_ui_patterns JS: pridané flip_box_count=document.querySelectorAll('.elementor-flip-box').length, has_flip_box; return objekt rozšírený o flip_box+flip_box_count; Python defensive defaults dopadnutý o 'flip_box': False. (2) Nová metóda _expand_flip_boxes(page)→int (pure CSS unflip, no DOM rewrite): na .elementor-flip-box parente nastaviť transformStyle:flat + perspective:none (zruší 3D kontext), na __layer--front transform:none + position:relative + opacity:1, na __layer--back transform:none + position:relative (NIE absolute) + display:block + height:auto + minHeight:150px + opacity/visibility:visible (back stack-uje pod front v normal flow → ceny + bullets viditeľné pre screenshot aj HTML scrape); __layer__inner/__overlay/__description force opacity/visibility/display. Vracia počet modifikovaných parent boxov; exception → 0. (3) _apply_ui_expansion: pridaný kľúč 'flip_boxes_expanded':0 do applied dict + nový krok 2.5 medzi accordion a carousel chain — `if allow_accordion and patterns.get('flip_box'): applied['flip_boxes_expanded']=await self._expand_flip_boxes(page)`. Gate: allow_accordion (complexity<0.7), NIE allow_dom_mutation (<0.3), pretože ide o čisto inline-style CSS aplikáciu — žiadne odstránenie/prestavba nodov, žiadny destructive rewrite. Pridané: tests/test_flip_box.py (3 testy: detect includes flip_box, LOW complexity triggers expansion called once, HIGH complexity skips — gate closed, method NOT called). NETKNUTÉ: render_page() / render_page_tiled() outer structure, _apply_ui_expansion carousel chain + complexity logic + safety gates, vision.py, knowledge_hub.py, extractors/**, Phase 2A engine (app/core/ingest_v2/**), requirements.txt, migrations/**, models/**. Tests: 130 passed (127 baseline + 3 nové), ~35s. SYNTAX OK, IMPORTS OK, routes: 20. Git: bez commitu (na želanie). Očakávaný impact: skakaciehradyorava.sk 14 produktov s 14 cenami (vs 2 doteraz); pre flip-box stránky +1 evaluate call (~50ms) v rámci existujúceho 600ms final settle, žiadny latency cost pre ne-flip-box weby (gate skip pri flip_box:False). Dalej: E2E re-run na skakacky, monitor Elementor-heavy stránok (screenshot pipeline re-meria scrollHeight po expansion → segmenty sa adaptívne menia). Viď claude_2b3_log.md.

---

2B-2: DONE ✅ — UI Expansion Layer (FIXED execution chain + complexity safety gate). Modifikované: app/core/browser.py — pridané metódy na koniec BrowserPool: _detect_ui_patterns (JS detekcia carousel_native/js/swiper_api/accordion/lazy_load/complexity_score, defensive→safe defaults complexity 0.5), _trigger_lazy_load (scroll bottom→top, žiadne slučky), _expand_accordions_strict (<details>.open + [role=button][aria-expanded=false].click() skip nav/menu/modal/dialog/dropdown, cap 20), _try_swiper_js_api (el.swiper.slideTo cyklus → späť na 0), _cycle_native_scroll (scrollLeft 0→33→66→100→0, no DOM mutation), _dom_fallback_expand_carousels (inline styly na .swiper-*/.slick-*/.owl-* slidy, LAST-RESORT), _apply_ui_expansion (orchestrátor). render_page_tiled: staré inline evaluate bloky (lazy skroll-loop + AOS + collapsibles + wait800) nahradené UI EXPANSION LAYER blokom (_apply_ui_expansion best-effort + AOS animácie zachované + wait800 + re-measure scrollHeight); cookie dismiss banner NETKNUTÝ. Pipeline (vždy rovnaké poradie): 1)detect 2)lazy load VŽDY 3)accordion ak complexity<0.7 4a)Swiper API 4b)native scroll 4c)DOM fallback IBA ak (4a+4b nezachytili) AND complexity<0.3. Gate: <0.3=allow_dom_mutation, <0.7=carousel+accordion bez DOM, >=0.7=iba lazy. Complexity = SPA/framework + canvas signály; carousel počet NIE je penalizovaný (e-commerce je cieľ). BUG FIX #1: DOM fallback triggers pre ANY carousel typ (JS aj native) keď prior steps nič neodcyklovali. BUG FIX #2: complexity už nepenalizuje carousel-heavy weby (odstránený swiper_count>3). Pridané: tests/test_ui_expansion.py (8 testov). NETKNUTÉ: render_page(), render_page_tiled štruktúra (len pridaný expansion krok pred screenshotmi), vision.py, knowledge_hub.py, extractors/**, Phase 2A engine (app/core/ingest_v2/** — renderer.py má vlastné _force_* helpery, nemodifikované), requirements.txt, migrations/**, models/**. Tests: 127 passed (119 baseline + 8 nových), ~40s. SYNTAX OK, IMPORTS OK, routes: 20. Git: bez commitu (na želanie). Dalej: deploy + E2E test na carousel-heavy webe (skákačky hrady) — overiť či DOM fallback zachytí 11 chýbajúcich produktov; merať latenciu (+~1-3s per page za carousel cyklovanie). Viď claude_2b2_log.md.

---

2B-1: DONE ✅ — Tiled (multi-viewport) screenshot pipeline pre Phase 1 vision. Modifikované: app/core/browser.py (+TiledPageResult dataclass, +render_page_tiled na konci BrowserPool), app/core/extractors/vision.py (+_extract_one_tile, +extract_page_with_tiled_vision; importy asyncio/TiledPageResult/merge_* hore), app/core/knowledge_hub.py (_ingest_url_vision: lightweight render → klasifikácia → render_page_tiled → extract_page_with_tiled_vision, s legacy single-screenshot fallbackom; dvojitý render+extract odstránený). Pridané: tests/test_tiled_vision.py (4 testy). Adaptive segmenty podľa scrollHeight: <3000=1, 3000-7000=3, 7000-12000=5, >12000=7. Overlap 22%, viewport 1280×2200 (pod 2400px Anthropic resize threshold). Parallel vision s Semaphore(3), prompt caching ZAPNUTÉ (use_cache=True). verify_product+verify_fact ostávajú POVINNÉ (anti-halucinácia). Reuse merger.py + verification.py — žiadna nová dedup/verify logika. NETKNUTÉ: render_page(), extract_page_with_vision() (legacy fallback), merger/verification/classifier/html_structured/types, _ingest_url_legacy, _persist_*, app/core/ingest_v2/**, requirements.txt, migrations/**, models/**. Tests: 119 passed (115 baseline + 4 nové). APP OK, routes count: 20. IMPORTS OK. Git: bez commitu (na želanie). Pozn: Phase 2A engine stále nepoužívaný pre product extraction (block_detection issue — riešiť neskôr). Dalej: deploy + E2E test tiled vision na reálnej dlhej stránke; sledovať cost (parallel vision × N segmentov).
